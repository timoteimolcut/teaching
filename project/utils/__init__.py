

"""
                         package
    inside one package:  multiple modules (at least one)
    inside one module:   multiple classe (at least one) or just defition code
    inside one class:    just defition code

"""

from .math_utils import PI, add, multiply
